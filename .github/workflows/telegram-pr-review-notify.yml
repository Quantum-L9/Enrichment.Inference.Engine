# --- L9_META ---
# l9_schema: 1
# origin: l9-template
# engine: enrichment
# layer: [ci]
# tags: [telegram, pr-review, integrations]
# owner: platform
# status: disabled
# --- /L9_META ---
# Telegram PR review notifications disabled.
# Previous triggers generated excessive operational noise.
# Workflow retained for future classifier-aware reactivation.

name: Telegram PR Review Notify

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  notify-telegram:
    if: false
    name: Post review activity to Telegram
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Disabled
        run: echo "telegram-pr-review-notify disabled"
