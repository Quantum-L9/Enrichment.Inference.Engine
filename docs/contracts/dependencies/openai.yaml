# ═══════════════════════════════════════════════════════════════
# Contract: OpenAI API Dependency
# Source:   app/services/openai_client.py
# Version:  1.0.0
# Updated:  2026-04-05
# ═══════════════════════════════════════════════════════════════

service_name: OpenAI
type: external
protocol: http
auth_method: Bearer token (OPENAI_API_KEY)
base_url: https://api.openai.com
base_url_env: null  # standard openai SDK

role: Secondary LLM provider (waterfall fallback or parallel variation mode).
# Resolved 2026-08-23: enrichment. app/services/enrichment/sources/openai_adapter.py
# registers OpenAISource in SOURCE_REGISTRY under "openai", so the WaterfallEngine
# dispatches to it as an enrichment source via OpenAIClient.complete_json(). It is not
# wired into consensus_engine.py.

